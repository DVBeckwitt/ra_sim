# Default hBN calibrant + dark frame paths for the CLI workflow.
# Update these to match your local files or pass a custom file via --paths-file.
calibrant: "/path/to/calibrant.osc"
dark: "/path/to/dark.osc"
